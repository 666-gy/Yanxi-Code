# 2026-07-04 工作日志

## 模型 fallback 修复
- 全局搜索 deepseek-chat 残留，全部替换为 deepseek-v4-flash
- 修改文件：CanvasPage.tsx、AgentPage.tsx（2处）
- 确认 store、SettingsModal、apiUsage.ts 均已使用 v4-flash/v4-pro

## Agent 工作区机制改造
- 移除左侧栏的工作区选择器
- 在输入框底部栏添加工作区 chip（Folder 图标 + 文件夹名/选择工作区），点击打开文件夹选择对话框
- selectWorkspace 函数增强：切换工作区时同步更新当前任务的 workspacePath
- 底部栏文案优化：chip 旁边显示"支持 / 指令 · # 文件引用"

## 用户更新概览（v1.2.8 → v1.2.9，用户自行修改）
- Agent 全面重构：多任务管理、文件操作、命令系统、文件引用、上下文获取、独立工作区、内置代码高亮
- 背景图功能：AppSettings 新增 backgroundImage/backgroundOpacity，主进程 select-background-image IPC
- 主题系统简化：移除浅色主题，固定暗色"现代黑"，Monaco 透明背景
- API 计费重构：按模型/功能分别统计，独立 apiUsage.ts 模块
- 设置面板重构：Tab 布局（模型配置/翻译设置/外观设置）
- Electron：单实例锁、Agent 上下文请求 IPC、多窗口 open-folder 支持
- 新增文件：theme/monacoTheme.ts、utils/apiUsage.ts、utils/codeExtractor.ts

## 完全移除 Agent 功能
- 用户决定沉淀研究 Agent 实现，先移除所有 Agent 代码
- 删除文件：src/pages/AgentPage.tsx
- 清理 App.tsx：移除 AgentPage 导入、路由 /agent、上下文请求监听器、Agent 消息监听器、Ctrl+Shift+A 快捷键
- 清理 Header.tsx：移除 Yan Agent 药丸按钮、Bot 图标导入
- 清理 electron/main.cjs：移除 agentWindow 变量、createAgentWindow 函数、toggle-agent-window/send-to-agent/send-to-main/request-context 四个 IPC handler
- 清理 electron/preload.cjs：移除 toggleAgentWindow/sendToAgent/sendToMain/requestContext/onContextRequest/sendContextResponse/onFromAgent 七个 API
- 清理 src/types/electron.d.ts：移除对应的 6 个类型定义
- TypeScript 编译零错误

## Linux 版本适配
- electron/main.cjs 4 处平台适配：
  1. `app.setAppUserModelId` 包裹 `if (process.platform === 'win32')`
  2. `createTray()` 包裹 try/catch，失败时 tray=null 不影响运行
  3. close 事件：仅在有 tray 时 hide-to-tray，否则直接退出
  4. 检查更新 User-Agent 动态适配：`YanxiCode/${version} (Linux; x64)` 等
- package.json build 段新增 linux 配置：AppImage + deb 双格式，Development 分类
- 前端代码零平台依赖，cross-env 无需添加
